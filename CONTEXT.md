# E2E 符号回归实验

对 Meta E2E 符号回归Transformer 在本地 PMLB 数据集上的推理与鲁棒性实验。

## Language

**模型树**:
模型在标准化输入空间中预测出的表达式，复杂度统计的唯一对象。
_Avoid_: 缩放前的树、unscaled tree、原生树

**原尺度表达式**:
把模型树经逆标准化改写回原始输入尺度的表达式，用于预测和展示，其中由逆标准化引入的缩放常数不计入复杂度。
_Avoid_: 缩放后的树、rescaled tree

**复杂度**:
表达式前缀遍历的节点个数，变量、常数、运算符、函数各占一个节点。统计模型时用模型树，统计真值时用化简后的真值公式，两者口径一致可直接比较。
_Avoid_: 节点数、长度

**噪声强度**:
乘性高斯噪声的标准差 sigma，施加后 y_noisy = y * (1 + xi)，xi 服从 N(0, sigma^2)。鲁棒性实验取 0、0.001、0.01、0.1 四档。
_Avoid_: 噪声水平、噪声比例

**数据集分组**:
按数据集名前缀分成 Feynman、Strogatz、Black-box 三组，前缀之外的全部归入 Black-box。
_Avoid_: 黑盒组、类别

**复原率**:
组内原始 r2 大于 0.9 的数据集占比，分母为剔除 skip 后的全部数据集数。
_Avoid_: 准确率、成功率
